---
import Layout from '../../layouts/Layout.astro';
import Hero from '../../components/Hero.astro';
import Section from '../../components/Section.astro';
import Card from '../../components/Card.astro';
import { getTranslations } from '../../i18n/translations';

const locale = 'en';
const t = getTranslations(locale);

const heroImage = '/images/eat/k1.jpg';

const menuCategories = [
  { key: 'appetizers', icon: '🥗' },
  { key: 'mains', icon: '🍽️' },
  { key: 'desserts', icon: '🍰' },
  { key: 'drinks', icon: '🍷' },
] as const;
---

<Layout 
  title="Menu | Vogthof Landgasthof"
  description="Fresh, seasonal ingredients prepared with passion."
  locale={locale}
>
  <!-- Hero Section -->
  <Hero
    headline={t.menus.hero.headline}
    subheadline={t.menus.hero.subheadline}
    backgroundImage={heroImage}
    ctaText={t.menus.hero.ctaPrimary}
    ctaLink="#daily-specials"
    ctaSecondaryText={t.menus.hero.ctaSecondary}
    ctaSecondaryLink="/en/restaurant"
    height="large"
  />

  <!-- Introduction from eat.md -->
  <Section
    title={t.menus.seasonal.title}
    subtitle={t.menus.seasonal.subtitle}
    background="cream"
  >
    <div class="max-w-4xl mx-auto text-center">
      <blockquote class="text-xl italic text-[#5C4033] mb-8 font-serif">
        "{t.menus.seasonal.quote}"
        <cite class="block text-sm mt-2 not-italic">— {t.menus.seasonal.quoteAuthor}</cite>
      </blockquote>
      
      <div class="prose prose-lg text-[#5C4033] mx-auto">
        <p class="mb-6">
          {t.menus.seasonal.paragraph1}
        </p>
        <p class="mb-6">
          {t.menus.seasonal.paragraph2}
        </p>
        <p>
          {t.menus.seasonal.paragraph3}
        </p>
      </div>
    </div>
  </Section>

  <!-- Menu Categories Section -->
  <Section
    title={t.menus.categories.title}
    subtitle={t.menus.categories.subtitle}
    background="old-lace"
  >
    <div class="grid md:grid-cols-2 gap-8">
      {menuCategories.map((category) => (
        <article class="bg-white rounded-lg p-8 shadow-md hover:shadow-lg transition-shadow">
          <div class="flex items-center mb-4">
            <span class="text-4xl mr-4">{category.icon}</span>
            <h3 class="text-2xl font-bold text-[#2C1810]">{t.menus.categories[category.key].name}</h3>
          </div>
          <p class="text-[#5C4033] mb-6">{t.menus.categories[category.key].description}</p>
          <ul class="space-y-3">
            {t.menus.categories[category.key].items.map((item) => (
              <li class="flex justify-between items-start border-b border-[#FDF5E6] pb-2 last:border-0">
                <div>
                  <span class="font-semibold text-[#2C1810]">{item.name}</span>
                  <p class="text-sm text-[#5C4033]">{item.desc}</p>
                </div>
              </li>
            ))}
          </ul>
        </article>
      ))}
    </div>
  </Section>

  <!-- Daily Specials Section -->
  <Section
    title={t.menus.dailySpecials.title}
    subtitle={t.menus.dailySpecials.availability}
    background="antique"
    id="daily-specials"
  >
    <div class="max-w-4xl mx-auto">
      <div class="bg-[#FFF8F0] rounded-lg p-8 mb-8">
        <div class="flex items-start gap-4 mb-6">
          <span class="text-5xl">🍽️</span>
          <div>
            <h3 class="text-2xl font-bold text-[#2C1810]">{t.menus.dailySpecials.title}</h3>
            <p class="text-[#5C4033]">{t.menus.dailySpecials.availability}</p>
          </div>
        </div>
        
        <div class="prose prose-lg text-[#5C4033]">
          <p class="mb-4">
            {t.menus.dailySpecials.description}
          </p>
          <ul class="list-disc list-inside space-y-2 mb-6">
            {t.menus.dailySpecials.includes.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
          <p>
            {t.menus.dailySpecials.cta}
          </p>
        </div>
      </div>
    </div>
  </Section>

  <!-- Allergen Information -->
  <Section
    title={t.menus.allergen.title}
    subtitle={t.menus.allergen.subtitle}
    background="cream"
  >
    <div class="max-w-3xl mx-auto text-center">
      <div class="bg-white rounded-lg p-8 shadow-md mb-8">
        <p class="text-[#5C4033] text-lg mb-4">
          {t.menus.allergen.text1}
        </p>
        <p class="text-[#5C4033]">
          {t.menus.allergen.text2}
        </p>
      </div>
    </div>
  </Section>

  <!-- View Full Menu CTA -->
  <Section
    title={t.menus.cta.title}
    subtitle={t.menus.cta.subtitle}
    background="old-lace"
  >
    <div class="text-center">
      <div class="flex flex-wrap justify-center gap-4 mb-8">
        <a
          href="/en/restaurant"
          class="inline-block px-8 py-4 bg-[#8B4513] hover:bg-[#A0522D] text-white font-semibold rounded-lg transition-all duration-300 shadow-lg hover:shadow-xl"
        >
          {t.menus.cta.ctaRestaurant}
        </a>
        <a
          href="tel:07361-73688"
          class="inline-block px-8 py-4 border-2 border-[#8B4513] text-[#8B4513] hover:bg-[#8B4513] hover:text-white font-semibold rounded-lg transition-all duration-300"
        >
          {t.menus.cta.ctaCall}
        </a>
      </div>
      <p class="text-[#5C4033]">
        {t.menus.cta.hours}
      </p>
    </div>
  </Section>
</Layout>
