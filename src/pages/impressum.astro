---
import Layout from '../layouts/Layout.astro';
import Section from '../components/Section.astro';
import { getTranslations } from '../i18n/translations';

const locale = 'de';
const t = getTranslations(locale);

const legalInfo = {
  owner: 'Reiner Ilg',
  address: 'Bergbaustraße 28/1, 73433 Aalen-Röthardt',
  phone: '+49(0)7361 73688',
  fax: '+49(0)7361 77882',
  email: 'info@aalen-vogthof.de',
  website: 'www.aalen-vogthof.de',
  taxId: 'DE14466139'
};

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'LegalService',
  name: 'Landgasthof Vogthof - Impressum',
  owner: legalInfo.owner,
  address: {
    '@type': 'PostalAddress',
    streetAddress: 'Bergbaustraße 28/1',
    addressLocality: 'Aalen-Röthardt',
    postalCode: '73433',
    addressCountry: 'DE'
  },
  telephone: legalInfo.phone,
  faxNumber: legalInfo.fax,
  email: legalInfo.email
};
---

<Layout 
  title="Impressum | Landgasthof Vogthof"
  description="Impressum und rechtliche Informationen des Landgasthof Vogthof in Aalen-Röthardt."
  locale={locale}
>
  <!-- Hero Section -->
  <Section background="paper" padding="medium">
    <div class="text-center">
      <h1 class="font-display text-4xl md:text-5xl lg:text-6xl font-semibold text-dark mb-4">
        {t.impressum.hero.title}
      </h1>
      <p class="font-body text-lg text-secondary max-w-2xl mx-auto">
        {t.impressum.hero.subtitle}
      </p>
    </div>
  </Section>

  <!-- Legal Content -->
  <Section background="base" padding="xlarge">
    <div class="max-w-4xl mx-auto">
      <!-- Angaben gemäß § 5 TMG -->
      <div class="bg-paper shadow-soft border border-light p-8 mb-8">
        <h2 class="font-display text-2xl font-semibold text-dark mb-6">
          {t.impressum.info.title}
        </h2>
        
        <div class="space-y-4">
          <div>
            <p class="text-sm text-muted mb-1">{t.impressum.info.owner}:</p>
            <p class="font-display text-lg text-dark font-medium">
              {legalInfo.owner}
            </p>
          </div>
          
          <div>
            <p class="text-sm text-muted mb-1">{t.impressum.info.address}:</p>
            <p class="font-body text-lg text-dark">
              {legalInfo.address}
            </p>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <p class="text-sm text-muted mb-1">{t.impressum.info.phone}:</p>
              <a href={`tel:${legalInfo.phone.replace(/\D/g, '')}`} class="font-body text-lg text-primary-600 hover:text-primary-700 transition-colors">
                {legalInfo.phone}
              </a>
            </div>
            <div>
              <p class="text-sm text-muted mb-1">{t.impressum.info.fax}:</p>
              <p class="font-body text-lg text-dark">
                {legalInfo.fax}
              </p>
            </div>
          </div>
          
          <div>
            <p class="text-sm text-muted mb-1">{t.impressum.info.email}:</p>
            <a href={`mailto:${legalInfo.email}`} class="font-body text-lg text-primary-600 hover:text-primary-700 transition-colors">
              {legalInfo.email}
            </a>
          </div>
          
          <div>
            <p class="text-sm text-muted mb-1">{t.impressum.info.website}:</p>
            <a href={`https://${legalInfo.website}`} target="_blank" rel="noopener noreferrer" class="font-body text-lg text-primary-600 hover:text-primary-700 transition-colors">
              {legalInfo.website}
            </a>
          </div>
          
          <div>
            <p class="text-sm text-muted mb-1">{t.impressum.info.taxId}:</p>
            <p class="font-body text-lg text-dark">
              {legalInfo.taxId}
            </p>
          </div>
        </div>
      </div>

      <!-- Haftung für Inhalte -->
      <div class="bg-paper shadow-soft border border-light p-8 mb-8">
        <h2 class="font-display text-2xl font-semibold text-dark mb-6">
          {t.impressum.content.title}
        </h2>
        <div class="font-body text-secondary leading-relaxed">
          <p>
            {t.impressum.content.text}
          </p>
        </div>
      </div>

      <!-- Haftung für Links -->
      <div class="bg-paper shadow-soft border border-light p-8 mb-8">
        <h2 class="font-display text-2xl font-semibold text-dark mb-6">
          {t.impressum.links.title}
        </h2>
        <div class="font-body text-secondary leading-relaxed">
          <p>
            {t.impressum.links.text}
          </p>
        </div>
      </div>

      <!-- Urheber- und Kennzeichenrecht -->
      <div class="bg-paper shadow-soft border border-light p-8 mb-8">
        <h2 class="font-display text-2xl font-semibold text-dark mb-6">
          {t.impressum.copyright.title}
        </h2>
        <div class="font-body text-secondary leading-relaxed">
          <p>
            {t.impressum.copyright.text}
          </p>
        </div>
      </div>

      <!-- Datenschutz -->
      <div class="bg-paper shadow-soft border border-light p-8 mb-8">
        <h2 class="font-display text-2xl font-semibold text-dark mb-6">
          {t.impressum.privacy.title}
        </h2>
        <div class="font-body text-secondary leading-relaxed">
          <p>
            {t.impressum.privacy.text}
          </p>
        </div>
      </div>

      <!-- Quick Links -->
      <div class="flex flex-wrap justify-center gap-4">
        <a href="/datenschutz" class="btn-primary">
          {t.common.footer.privacy}
        </a>
        <a href="/menus" class="btn-secondary">
          {t.common.nav.menus}
        </a>
        <a href="/tagesessen" class="btn-secondary">
          Tagesessen
        </a>
      </div>
    </div>
  </Section>
</Layout>
